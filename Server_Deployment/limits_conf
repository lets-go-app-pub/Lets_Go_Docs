# -f (file size)
* soft fsize unlimited
* hard fsize unlimited

# -t (cpu time)
* soft cpu unlimited
* hard cpu unlimited

# -v (virtual memory)
* soft as unlimited
* hard as unlimited

# -l (locked-in-memory size)
* soft memlock unlimited
* hard memlock unlimited

# -n (open files)
* soft nofile 64000
* hard nofile 64000

#-m; No effect on Linux systems with kernel versions more recent than 2.4.30.

# -u (processes)
* soft nproc 64000
* hard nproc 64000

